package org.example.feedbackservice.validation.defects;

import org.example.feedbackservice.validation.DefectId;

public enum StringDefects implements DefectId {
        INCORRECT_LENGTH,
        CONTAINS_FORBIDDEN_CHARACTERS
}
